#!/usr/bin/env ruby

# Inspired by http://timelessrepo.com/making-ruby-gems
begin
  require 'riff'
rescue LoadError
  $LOAD_PATH.unshift "#{File.dirname(__FILE__)}/../lib"
  require 'riff'
end

riff = Riff.new()
STDIN.each do |line|
  riff.handle_diff_line(line)
end
